kind: Service
apiVersion: v1
metadata:
  name: visit-counter-service
spec:
  type: NodePort
  selector:
    app: visit-counter-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080